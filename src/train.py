"""
Training entry point for the skin lesion classification model.

The final training pipeline should be added once the existing project
notebook/model code is cleaned and moved into reusable scripts.
"""


def main() -> None:
    """Run the model training pipeline."""
    print("Training pipeline placeholder.")
    print("Add dataset loading, model definition, training loop, and checkpoint saving here.")


if __name__ == "__main__":
    main()
