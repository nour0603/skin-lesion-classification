# Notebooks

This folder is reserved for experiment notebooks used during model development.

The main project workflow has been refactored into reusable Python modules under `src/`, so notebooks are treated as supporting exploratory material rather than the primary implementation.
